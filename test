#include <stdio.h>

int main() {
    int number, square;

    // 사용자로부터 숫자 입력받기
    printf("숫자를 입력하세요: ");
    scanf("%d", &number);

    // 입력받은 숫자의 제곱 계산
    square = number * number;

    // 결과 출력
    printf("당신이 입력하신 숫자의 제곱은 %d입니다.\n", square);

    return 0;
}

